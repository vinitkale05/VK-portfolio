{
  "name": "pranav-gawai-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "dev:api": "vercel dev --listen 3001 --yes",
    "build": "npm run build -w frontend",
    "preview": "npm run preview -w frontend",
    "test": "vitest run"
  },
  "dependencies": {
    "@clerk/backend": "^1.21.0",
    "@vercel/og": "^0.11.1",
    "groq-sdk": "^1.1.1",
    "mongodb": "^6.12.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
